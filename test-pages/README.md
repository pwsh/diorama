# Deterministic 3D test pages

Headless-verification harness for `ThreeDRenderer`. Each page imports the
built renderer chunk directly, constructs a scene from inline fixtures, and
(usually) drives `updateTargets` with a **synthesized `performance.now`
clock** so animation states (walking, sitting, activities, bed covers,
bubbles, despawn fades) are deterministic. Results are exposed two ways:
`document.title` carries machine-checkable assertions; the canvas carries
the visual.

## Running

```bash
npm run build
mkdir -p /tmp/diorama-harness && cp -r dist/* test-pages/* /tmp/diorama-harness/
cd /tmp/diorama-harness && python3 -m http.server 8931 &

# assertion dump
google-chrome --headless=new --window-size=1280,880 --virtual-time-budget=9000 \
  --disk-cache-size=1 --dump-dom "http://localhost:8931/mega-test.html?b=$RANDOM" \
  | grep -o "<title>[^<]*</title>"

# screenshot
google-chrome --headless=new --window-size=1280,880 --hide-scrollbars \
  --virtual-time-budget=9000 --disk-cache-size=1 \
  --screenshot=out.png "http://localhost:8931/mega-test.html?b=$RANDOM"
```

Gotchas: always pass a cache-busting query (`?b=$RANDOM`) AND, when
iterating on the renderer, bust the module import too
(`import('./assets/three-renderer.js?v=' + Date.now())`) — Chrome caches
module subresources aggressively. `--virtual-time-budget` is required or
the screenshot fires before first render.

## Key pages / baselines

| Page | Asserts (title) |
|---|---|
| mega-test.html | seating: `sit=1.00 dwell=7.0 spot=true spots=6` (seating v2: the 2000 mm sofa registers 3 spots, + toilet/ottoman/stool) |
| seating-test.html | `SEATING PASS` — v2 claims/multi-seat/approach: two rigs on one chair → exactly one sits (roots ≥ 300 mm apart); 2400 mm sofa → 4 spots, three rigs sit at distinct spots ≥ 500 mm apart; approach-from-behind never captures until the raw pos is in front; 1500 mm bed → 2 lie-lanes, third occupant stands |
| pathfind-test.html | `PATH PASS cross=true[door-x] sofa=true(0) arrive=true` + nav samples |
| phase4-test.html | all solo activities engage, shower `priv=1.00` |
| phase5-test.html | `?scene=a` eat/work/tv seated; `?scene=b` bed covers hide rigs |
| phase6-test.html | `PHASE6 PASS` — committed bubble is drawn FROM the expected weighted POOL per scene/tb (kitchen-night/morning, seated-evening, bed), or suppressed (`-`) when an activity engages |
| event-bubble-test.html | `EVENTBUBBLE PASS n/n` — event-focused bubbles (Phase 2a). Drives BOTH the real `Planner` (bundled `planner.mod.js`, faked `Date.now`) — appliance ≥5 min run→finished fires once / short run + fast blip do NOT fire / pause keeps the run alive / rain-start fires once / cloudy→lightning = severe / alert empty→warning = severe_alert — AND the renderer event tier (`three-renderer.mod.js`, faked `performance.now`) — event beats seated-evening, expires after `EVENT_TTL_S`=40 s, per-rig stagger (idleOffset) means two rigs don't adopt the same frame, far appliance event is distance-gated. Bundle both via esbuild like config-test/rooms-test (see the page's build comment) |
| avatar-bubble.html | `BUBBLE PASS` — walk: committed chatter glyphs ∈ the kind's `AVATAR_BUBBLES` pool; wash: an engaged activity suppresses the bubble |
| fidget-test.html | `FIDGET PASS` — wave-on-spawn fires + ≥3 distinct idle one-shots, all from the known 8-fidget set (`?test=stretch\|phone\|wave` freeze a pose) |
| avatar-anim-test.html | `AVATARANIM PASS n/n` — Phase 4b animated appendages (`AvatarPrimitive.animate` sway/flap/orbit/spin: sinusoidal + phase independence, flap 2× speed while walking, orbit constant radius, spin monotonic), gait cycles (`gait` hop = phase-locked legs + bigger bob; knuckle = torso pitched + arm>leg amplitude), quad `earAnimate: 'swivel'`, and a defaults-byte-match guard (adult walk joints == the classic formulas) |
| ai-test.html | AI avatar wanders in-region, engages, presence-off slow fade |
| avatar-lineup.html / newkinds-test.html | all avatar kinds render |
| tabletest.html | table clearances: `rootEdge=190`, hands AND elbows above `top − 10` across adult+child × eat/work/tall (`dHand*`/`dElb*` all ≥ 0) |
| glass-test.html | `ghosts=1 wallOps=[0.06,0.06,0.22,0.22]` (cutaway) |
| cutaway-persist-test.html | `CUTAWAY PASS 41/41` — the cutaway fade SURVIVES a `_keyFloor` rebuild (the "foreground walls flash translucent → opaque → translucent while sitting still" bug: any `switch.*`/`number.*` change in HA bumps configRev → full floor rebuild → walls reborn at build opacity 0.45 / stairs at 1.0, then a ~0.36 s ease back down). Pins: an identical-data `updateFloor` never births a settled faded wall OR stairs flight brighter than its pre-rebuild opacity (nor on any of the next 60 frames); the ease-back-UP direction resumes mid-climb and stays monotonic; a genuinely NEW wall id still births at `baseOpacity` while its neighbours stay carried; removing a wall PRUNES its key (map size == live mesh count); `clearTransientGroups` (floor switch) clears both fade maps so the next build is fresh; ghost walls get the same continuity under glassHouse and prune when glass house is turned off; a glass-house toggle that LOWERS the build opacity clamps the carried value to the new base; and the cutaway predicate itself never oscillates over 150 static-camera frames (proving the rebuild was the only flash source). **T8/T9 (2026-08-06) pin the ease as dt-BASED** — it was a fixed per-FRAME factor whose real time constant scaled with the refresh rate (≈0.16 s at 60 fps but ≈0.08 s at 120 Hz and ~2 s at 4.5 fps, measured): 0.5 simulated seconds subdivided at 30 fps vs 120 Hz must cover the SAME fraction of the gap (per-frame easing gave 79.4 % vs 99.8 %) and that fraction must equal the analytic `1 - exp(-t/τ)`; a bare `_updateWallCutaway()` still means exactly one 60 fps frame == the legacy 0.1 step (which is why T1–T7 are byte-unchanged); a 5 s tab-resume gap eases exactly the 0.1 s clamp, a non-finite dt falls back to 60 fps, a negative dt is inert |
| step-test.html / stepneg-test.html | step-light pools front-only; sunken wash down |
| layers-test.html | `?nofurn=1` hides furniture+lights in 3D |
| layers2d-test.html | `LAYERS2D PASS n/n` — the **2D** layer surface (distinct from layers-test's 3D group flips). Offscreen-canvas pixel work (the ruler-dims §11M/§12 technique): (1) `Scene3D.floorColor`/`floorTex` + the per-floor `look3d` override now paint the 2D floor (they only ever reached the 3D slab) — colour match, override precedence, per-kind texture pixel deltas, cached-tile determinism; (2) `labels` hides room AND ground-area/pool names while their PAINT survives; (3) `objectLabels` hides door pills / furniture labels / fixture NAME captions while VALUE readouts (env chip, state badges) are byte-identical; (4) the new `openings` + `peekFloors` keys hide their content and stop capturing input; (5) `LAYER_DEFS`/`LAYER_CATS`/`layerDefsByCat` metadata; (6) the REAL sidebar renders one `.layer-cat` caption per category. NB `drawAll` carries ctx state between calls, so the page paints TWICE per snapshot — a first paint onto a virgin context is not the steady state. Bundle via the sensor-focus recipe (`test-pages/layers2d-entry.ts`; tsconfig + lit aliases) |
| rooms-test.html | `ROOMS PASS n/n` — real export: ≥4 loops, 4 anchors in 4 distinct loops (25 mm weld heal). Uses `geometry.mod.js` (`npx esbuild src/geometry.ts --bundle --format=esm --outfile=<harness>/geometry.mod.js`) |
| align-guides-test.html | `ALIGNGUIDES PASS n/n` — the UNIVERSAL cross-category alignment guides. Pure `nearestAlign` / `bestAlignShift`; the shared `ALIGN_DRAG_KINDS` / `ALIGN_POLY_DRAG_KINDS` membership matrix (snapper + painter read ONE set); a furniture centre snapping to a WALL VERTEX; the 8 px tolerance boundary (7 px yes / 9 px no); self-exclusion (a shape never aligns to itself); a wall vertex snapping to a ROOM ANCHOR and overriding the 15° angle lock, with the release WELD still winning; Alt suspending alignment on both a wall vertex and furniture; roomAnchor / polygon-vertex / bbox-centre body-move / whole-wall-move behaviours; the documented candidate-pool composition (one of EVERY source) incl. the poly-only vertex extension; snapshot-once discipline + big-floor cost; locked items never moving; and a PIXEL check that the dashed guide column paints while dragging and is gone after release. Bundle: real Planner + real canvas-interact + drawAll from one entry (the wall-edit-test recipe) |
| identify-test.html | `IDENTIFY PASS n/n` — Alt+click IDENTIFY. Locked furniture / locked wall / ground-area sweep hits with the right kind+label+locked+anchor and NO side effect (an unbound switch does not toggle, a plain click still does); Alt+DRAG stays free placement (identify only resolves on an UNMOVED press); `identifyActive()` TTL; edit-only refusal in kiosk; Alt dblclick opens no binding modal; the 3D contract (`Planner.identifyItem(kind, id)` — the same entry point three-view's raycast-kind map calls); and the REAL sidebar navigation — section slug AND `<slug>/<roomId>` sub-key expanded, the `data-item-row` row rendered + flagged `identify-flash`, no focus steal, selection still hot, and a second identify re-navigating. Bundle: sensor-focus recipe (tsconfig + lit aliases) |
| wallsnap-test.html | `WALLSNAP PASS n/n` — pure `snapToWallEdge` (both sides, h/v/diag), fireplace flush offset 275, switch wall-lock + `gangSlot` ganging, `nearestAlign` smart-guide selection. Uses `geometry.mod.js` (same esbuild transpile as rooms-test) |
| sliver-test.html | `SLIVER PASS n/n` — region-size-aware `_nearestFreeCell`: AI spawns in the open room, not the sliver behind furniture |
| nav-parity-test.html | `NAVPARITY PASS n/n` — **the differential guard for `scripts/floorplans/physical.mjs`**, whose `buildNavGrid` is a hand-written replica of the renderer's private `_buildNav` (the floorplan validator's checks 9–12 stand on it). Runs BOTH over 7 fixtures — multi-room shell w/ doors+windows, furniture exempt-list + custom recipe, pool+void, fences/gate, sliver channel, sunken-stairs rails, diagonal pinch — and asserts identical grid dims, blocked bitmap (cell for cell), blockedCount, region labels and region sizes, plus analytic `NAV_CELL` / `PERSON_R` / `WALL_HALF+PERSON_R` probes. **Change `_buildNav` → run this page.** Needs `geometry.mod.js` AND `physical.mod.js` (`npx esbuild scripts/floorplans/physical.mjs --bundle --format=esm --outfile=<harness>/physical.mod.js`) |
| window-test.html | `WINDOW PASS n/n` — per-kind pane counts (single/double_hung/casement_pair/sliding/picture), open behaviour per kind, custom sill/height moves the glass + wall sub-sill/header cut boxes (asserts mesh Y bounds) |
| bubble-anchor-test.html | `BUBBLE PASS n/n` — thought bubble anchored per-rig off the plumbob (child < supermodel, above head top, adult ≈ 2462) + stays 460 mm above the plumbob and drops in world space when seated |
| fireplace-wall-test.html | `FIREPLACE PASS` — mantel back flush with the firebox back plane (D2/2 = 225), never proud (skips inverted-hull outline shells via `userData.outline`) so a wall-snapped fireplace doesn't poke through |
| flights-test.html | `FLIGHTS PASS 686/686` — roadmap P4 flights & ISS. Pure `flights.ts` (normalizer over a REAL 94-aircraft airplanes.live capture + hand-authored readsb/garbage fixtures in `fixtures/adsb/`: `alt_baro:"ground"` filtered, no-position/string-lat rejected, `{ac}`/`{aircraft}`/bare-array envelopes equivalent, dbFlags bit-1 military, alt_geom fallback; compression curves monotonic + bounded; bearing/distance goldens + a great-circle cross-check) + `sky-astro.satAltAz` goldens (overhead ≈ π/2, antipodal < 0, cardinal azimuths) + the REAL `Planner` over a fake HaApi with `globalThis.fetch` stubbed (poll → status/rev, disabled/no-origin are inert with ZERO fetches, geo-fit origin beats `weather.lat/lon`, radius+altitude filters, nearest-first `MAX_AIRCRAFT` cap, failed poll keeps last data, `entity` source reads attributes with no timer, `setFlights` round-trips through `_normalizeStore`). **Wave-1 field/model enhancement** (research `flight-fields-models.md`): registry enrichment extracted from the real capture (`reg`/`typeCode`/`typeDesc`/`operator`/`squawk` counts, LADD entries keep their registration, `emergency:"none"` collapsed to null), the **non-aircraft filter** (`C*`/`B3` — the fixture's three `C2` LAX ramp vehicles, plus synthetic AIRBORNE C0–C5/B3 that the ground sentinel cannot catch, while B1/B2/B4/B6 stay), the full `dbFlags` bit matrix (1/2/4/8 + combinations + non-numeric), `isEmergency`/`emergencySquawk` (enum superset vs. the three 7×00 squawks), the **`aircraft-types.ts` archetype golden matrix** (every §3.5 designator asserted against an independently transcribed expected map, two-way coverage vs `TYPE_ARCHETYPE`, the CRJ/ERJ-vs-E-Jet and PC-12-vs-Caravan and 757-at-A4 spotlights, the §3.6 category ladder, garbage-never-throws, and `legacyModelKind` agreeing with the shipped 3-way `aircraftModelKind` on every category), `sanitizeLabelFields`, the **elevation-true altitude cap** (§6b — the fix for "every plane hangs directly above the property": `dispY = max(yMin, min(compressAltitudeMm(alt), r·altM/distM))`, goldens at 35,000 ft @ 8.7 / 10 / 15 nm and 5,000 ft @ 0.5 nm, the displayed ELEVATION ANGLE strictly decreasing with distance at fixed altitude — swept at radius 5/15/30/100 — while both the radius and (under the radius-anchored mapping, P > 1) dispY itself grow, non-decreasing in altitude at fixed distance, the displayed elevation angle EQUAL to the true one on the elevation branch and never exceeding it above the `clearMm` floor, the log-curve branch still reachable at SMALL radii, floor + garbage tolerance on the shared `flightDisplayAltitudeMm`, and `FLIGHTS_DEFAULT_RADIUS_NM === 15`), the **display-shell rescale** (§6c/§6d — the fix for "planes 7 nm away are directly over the property": `rMaxMm` 24000 → **120000** + `yMaxMm` 22000 → **66000** with `yMinMm`/`clearMm` unchanged, so the shell reaches 120 m — a 7 nm aircraft at the default radius lands 32.6 m out (27 % of the shell), asserted altitude-INDEPENDENT — while `yMaxMm` is sized so the elevation branch governs cruise traffic from ≈7.4 nm outward (swept 8–60 nm); every 9b golden recomputed (radii exactly 5×, display ANGLES unchanged because the elevation branch is angle-true at any shell size), plus `FLIGHT_SHELL_REACH_MM = hypot(rMax, yMax)` and `flightDisplayScale` (monotonic, bounded [1, 1+gain], 1 at the origin, saturating past `rMax`, garbage-safe — a far aircraft renders LARGER for legibility)), the **radius-anchored radial remap** (§3/§6b/§6c/§6d — the fix for "flights 10 miles away still appear close to the property line": the asymptotic `rMax·d/(d+K)` curve, which front-loaded distance and could never reach the rim, is replaced by `rMax·clamp(d/R, 0, 1.05)^P` with `P = ln2/ln1.5 ≈ 1.7095` DERIVED from the user's two anchors — `d = radiusNm` renders AT the rim and `d = ⅔·radiusNm` at the shell midpoint, both asserted EXACTLY at radius 5/15/30/100 along with full radius-invariance of the curve in `u = d/R`, so the drawing distance matches the radius entry at any setting; plus the 1.05 dead-reckoning headroom (keeps expanding just past the rim, then pins), a zero/garbage radius falling back to `FLIGHTS_DEFAULT_RADIUS_NM` instead of dividing by zero, 10 nm landing at 50 % of the shell where the old curve put it at 71 %, and `FLIGHT_SCALE_GAIN` eased 2.2 → **0.8** so a rim aircraft reads "fairly small" at 1.8× rather than 3.2×)), and the **emergency alert** (severity `error`, ignores the 3 nm overhead gate, survives the 15 min prune while still declaring while reporting "unchanged" so routine polls stay LIVE-path, dismissal re-arms after 10 min, never the one dropped by the newest-wins cap). **User glow rules** (research `flight-glow-rules.md`): `globMatch`'s hybrid wildcard (plain text = substring, any `*`/`?` = ANCHORED, `?` = exactly one char, case-insensitive both ways, blank pattern = "any", null value only satisfies a blank pattern) plus its **no-regex-injection** guarantee (a 12-entry metacharacter corpus — `(((`, `[a-z`, `\`, `a|b`, `(a+)+$`, `\d{3}` … — never throws, matches itself LITERALLY, and a catastrophic-backtracking shape is inert); `matchesGlowCriteria` field by field (6 wildcard strings, 3 numeric min/max pairs incl. inclusive ends + a NULL live field FAILING rather than passing, 5 tri-states incl. `emergency` reading through `isEmergency`, AND across all present criteria, empty criteria = catch-all) exercised against the REAL 94-aircraft capture; `resolveFlightGlow`'s three tiers (master `beacons` gate silences user rules too; EMERGENCY outranks a matching rule's pattern AND colours; first ENABLED match wins and REPLACES the default — reordering the same two rules flips the answer, a disabled rule is skipped, `pattern:'none'` resolves to null and does NOT fall through; no rules = the shipped interesting/military/LADD ladder byte-for-byte); `flightGlowFrame` goldens per pattern (`flash` reproduces the SHIPPED 1.2 Hz envelope to 1e-12 across a 5 s sweep, `strobe` fires exactly TWO pops at phase 0/0.12 with a binary mix, `rotate` never drops below its 0.35 floor, `fade` has a 5 s period with mix tracking brightness, `alternate` hard-swaps every 0.8 s at constant brightness, all 7 bounded 0..1 over 12 s, non-finite `t` never NaN); `lerpHexColor`; and `sanitizeFlightGlowRules` (unknown pattern / colourless visible pattern drop the rule, `'none'` keeps none, colours normalize to lowercase `#rrggbb`, **a blank text criterion collapses to `undefined` and NEVER to a match-everything `**`**, uppercase-in-the-data fields uppercased while free text keeps its case, numeric clamp + swap-if-inverted, 30-rule cap keeping the first 30, deterministic id repair + de-dup, idempotent + JSON-round-trippable), plus the `setFlights` store-boundary round-trip. Bundle: `flights.ts` + `aircraft-types.ts` plain (both zero-import), `sky-astro.ts` `--bundle`, planner per the config-test recipe |
| flights-render-test.html | `FLIGHTSRENDER PASS 350/350` — roadmap P4 renderer + 2D (wave-2 enhanced). Drives `updateFlights` / `updateIss` / `_advanceFlights(dt)` directly with synthetic `FlightPoint`s: rig lifecycle (diff-by-hex, dying fade over `FLIGHT_FADE_S`, disposal detaches the asm + frees the per-rig label/id CanvasTextures, mid-fade re-acquire revives the SAME rig), **8-way archetype models** (ga-high wing ABOVE the fuselage centerline vs ga-low BELOW, twin-prop's 2 spinning nacelle props, turboprop + bizjet T-tails, bizjet REAR pods, widebody strictly bigger than narrowbody, heli Y main + X tail rotor, military tint ≠ civil, archetype/military/privacy change rebuilds the rig IN PLACE keeping motion state), **fuselage identifier planes** on both flanks (un-mirrored orientations asserted), **status beacons** (priority emergency red > interesting yellow > military green > LADD white, ~1.2 Hz pulse in `_advanceFlights`, `beacons:false` tears down), **privacy dim** (PIA/LADD at 0.45 opacity, PIA identity blanked to hex, no PIA banner), **labelFields plates** (field composition + repaint-on-change), label policy (ga-piston+callsign → towed banner; everything else → sprite plate; `showLabels:false` tears both down), placement (fresh rig snaps to `flightDisplayPos`, anchor moves `_flightsGroup` not the rig offsets), **distance-compensated model scale** (§4b — the rig GROUP scale is `fade × flightDisplayScale(displayRadius)`: asserted exactly on build, on rebuild and every `_advanceFlights` frame, uniform on all three axes, a rim rig >1.5× a near one, a near rig still ≈1×, capped at 1+gain, and the FADE **composes** — a dying rim rig shrinks from its enlarged size, never overwritten by either term) plus the **user model-size preference** (§4b-2 — `FlightsConfig.modelScale` folded into the SAME `_flightRigScale` site as a THIRD multiplicative term: absent/1 reproduces the shipped size exactly, a live rig picks a new factor up on the next FRAME (a poll is a correction — only build/rebuild set scale directly), a fresh rig and a model rebuild both build at the multiplied size, a fading rig still shrinks from it, and 0.5..4 clamping + NaN/negative/non-number → 1), the **camera-frustum reach requirement** (§4c — aircraft join the neighborhood overlay's dynamic frustum: on-screen aircraft widen `far` past the stock 150000 with `near = far/15000`, the recorded value IS `FLIGHT_SHELL_REACH_MM`, `controls.maxDistance` stays STOCK (the documented asymmetry — flights never stretch the orbit range), hiding the Flights layer restores the stock triple while the rigs stay alive, unhiding re-widens, and an empty list / a lost geo origin restore it EXACTLY), heading (north-bound at θ=0 → nose at scene **+Z**, θ=π/2 → +X, a 90° track change EASES), dead reckoning, pitch sign, ISS (zenith at the 26 m shell, below-horizon/no-observer hidden, ramp-in, camera-recentered, fix-pair rates), layer gating both ways, poll-correction ease. **Wave-3 display**: **optional tow banners** (`FlightsConfig.banners`, absent = ON — `false` tears a live banner down, frees its shared map exactly once, falls back to the label plate and keeps the fuselage identity; `showLabels:false` still kills both; an airliner never towed one either way), the **fuselage text layout** (a LARGE-fuselage archetype with an operator paints the OPERATOR on both flanks and the IDENTIFICATION on the spine — 3 marking planes, flanks still sharing one material/map, the spine plane carrying its OWN texture, sitting just above the body skin, centred on the axis, facing UP, with local +X → world +Z and local +Y → world +X so it reads nose-to-tail from above; NO operator falls back to identity-on-flanks with no spine plane; ga-high/ga-low/heli keep the flanks-only layout even when an operator IS known; turboprop/twin-prop/bizjet take the two-marking layout; a PIA aircraft shows the hex and no operator on any surface; a long operator SHRINKS to fit rather than squashing — asserted against the texture's own aspect), **fog exemption** (EVERY material on a rig — airframe, glass, inverted-hull outline shells, banner — has `fog:false`, the shells using a per-model CLONE so the SHARED `_outlineMaterial` still fogs for ground geometry; privacy-dimmed builds too), and **clickability** (the rig ASSEMBLY carries `userData.kind='flight'` + hex, a real `_raycastFixture` through a camera aimed at the rig surfaces `{kind:'flight', hex}` from the airframe AND from the label sprite, a hidden Flights layer makes aircraft untappable, and a model rebuild RE-tags the new assembly). Bundle: `flights.ts` + `aircraft-types.ts` plain (zero-import), `sky-astro.ts` `--bundle`, **User glow rules**: a matched rule builds the bead/halo in ITS colour + pattern (the `pattern|colorA|colorB` rebuild key, persistent per-rig `THREE.Color`s, an unchanged resolution re-polled does NOT rebuild the objects), each pattern sampled on the LIVE materials through real `_advanceFlights` frames (`strobe` = two opacity pops per second with colour A on the first and B on the second, halo recoloured in lockstep; `rotate` never below 0.35 and crossfading THROUGH the pair; `alternate` hard-swapping A→B→A at constant opacity; `solid` static two-tone — colour A bead, colour B halo, no lerp armed, opacity never animating; `fade` peaking/bottoming on its 5 s period), `'none'` removing bead+halo entirely without disturbing a non-matching neighbour, an UNMATCHED aircraft keeping the default ladder with its flash envelope **byte-identical to the shipped beacon (<1e-12 over 60 frames)** and never repainting `.color`, EMERGENCY overriding a matching rule end-to-end, `beacons:false` silencing rules too, re-resolution on a poll/rule-list edit **without rebuilding the rig** (motion state + phase survive), first-match/reorder/disable through the renderer, a garbage rule list re-sanitized renderer-side, a model rebuild re-attaching the RULE glow to the new assembly (and a beaconless rig staying beaconless), the despawn fade still scaling the glow, and **zero per-frame allocation** (materials + the three persistent Colors + both `.color` instances identical after 120 frames, the source Colors never mutated by the lerp, phase accumulating in SECONDS unwrapped). Bundle: plus `dist/*` copied beside the page (imports the BUILT renderer) |
| flights-ui-test.html | `FLIGHTSUI 295/295` (title has no "PASS" word — counts-equal means pass) — roadmap P4 settings/alerts UI layer (wave-2 enhanced). Pure `alerts.ts`: the back-compatible `buildAlertFeed(..., extra?)` channel (client-local alerts appended verbatim, sorted with the rest, NOT subject to per-source toggles or the Repairs severity floor; omitting the arg reproduces the old feed exactly). Then the REAL `Planner` over a fake HaApi with `globalThis.fetch` stubbed and a frozen/advanceable `Date.now`, driving `_computeFlightAlerts()` directly: low-overflight (3 nm overhead gate, hex-keyed 10 min cooldown, no-callsign falls back to the uppercase hex), watch list (callsign PREFIX + exact hex, re-uppered at match time, 30 min replace-not-stack re-fire), ISS pass as a below→above 10° EDGE detector, dismissal (local mute, no service call, view mode refuses), the 15 min retention prune, the house-wide `flyover` household event, `setFlights` watch normalization + `labelFields` sanitization round-trip + **`modelScale` sanitization** (0.5..4 clamp, NaN/negative → default, exactly-1 normalizes back to absent), feed integration. **Wave-2 UI/2D**: the REAL settings drawer's Flight-tracking block (Status-beacons + privacy-dim checkboxes + the 9-checkbox Label-fields grid round-trip through `setFlights`, plus the **Model size ×** number input — typing writes the store, an out-of-range value is clamped by `setFlights`, exactly 1 clears the field), and `drawFlights` 2D assertions via the bundled `canvas-render.ts` (priority beacon ring color, privacy dim alpha, labelFields-driven text via the exported pure `flightBeaconColor`/`flightFieldText`/`flightLabelLines`). **Wave-3**: `banners` config round-trip + its "Tow banners (small planes)" drawer checkbox (nested under the labels master, opt-OUT), `Planner.flightByHex` (case-insensitive, misses cleanly, tolerates a null feed), the **2D pick path** (`drawAll` publishes `flightHitPx` per aircraft, `hitFlight` resolves the dart plus a touch margin, nearest wins an overlap, hiding the `flights` layer EMPTIES the map so nothing is pickable, a disabled feature never picks) and **click routing** through the real `onCanvasClick` (edit+select and kiosk both dispatch `open-flight-info`, view mode refuses, a placement tool is not hijacked), and the **`<diorama-flight-modal>` field matrix** (callsign headline, hex, registration, operator, type code + long description, archetype + ADS-B category, REAL altitude with a climb/descend arrow, ground speed, vertical rate, track + compass point, squawk, distance + bearing from home, fix age; EMERGENCY/MILITARY/INTERESTING/LADD chips; PIA anonymization withholding callsign/registration/operator while keeping type + category and explaining itself, reversed by `privacyDim:false`; "signal lost" keeping the last frame when the aircraft leaves the feed — including a null feed; sparse aircraft omit rather than blank-label their missing fields; live-channel refresh; a closed or unknown card renders nothing). **Background-text rows (Settings ▸ Display, the only page covering that editor)**: the banner-mode **Aircraft** picker (9 options = the 8 flight archetypes + the classic tow plane, writes/clears `BgTextEntry.aircraft`, passed through by `bgTextsResolved`, absent on non-banner modes) and the per-entry **Model size ×** input (writes/clamps 0.5..5, exactly-1 clears, present on every mode, passed through by `bgTextsResolved`). Bundle: `alerts.ts` `--bundle`; `canvas-render.mod.js` is now a COMBINED bundle of canvas-render + canvas-hit + canvas-interact from one temp entry (they must share module state — `flightHitPx` is written by `drawAll` and read by `hitFlight`); **Glow rules** (research `flight-glow-rules.md`): the **2D ring** driven through the REAL `drawAll` over the recording context with `performance.now` stubbed — a matching rule recolours the ring (and the default green disappears), `solid`+colour B draws BOTH rings (0.6 core / 0.35 halo), `alternate` hard-swaps on the wall clock, `rotate` crossfades to a colour that is NEITHER of the pair and never drops below its 0.35 alpha floor, the ring alpha IS the shared `flightGlowFrame(...).alpha`, `strobe` is bright only in its two narrow pops (A then B) and dark for most of the cycle, `'none'` draws no ring while the aircraft still draws, emergency + `beacons:false` both override a matching rule, and a garbage rule list never throws in the RAF path — plus `flightBeaconColor`'s optional rules argument. The **rules editor** in the real settings drawer (`data-glow-rule` / `data-glow-field` / `data-glow-add` hooks): add appends an EXPANDED rule that survives the sanitizer (visible pattern + a colour + enabled + empty catch-all criteria), the summary line shows the name or a criteria digest and an `[off]` tag, every criteria input round-trips through `setFlights` (label trimmed, `typeCode` uppercased, a blank text field or a blank numeric bound CLEARS rather than authoring a match-all, tri-state Any/Yes/No), the 7-option pattern dropdown, both colour pickers + the colour-B clear, the colour row hiding for `'none'`, the enable checkbox, collapse/re-expand preserving values, **▲/▼ reorder** (ends disabled, order written to the store, rules preserved), `✕` delete (last one clears the field entirely), and the 30-rule cap disabling the add button. Bundle: planner per the config-test recipe, `flightsui.mod.js` per the sensor-focus recipe (tsconfig copy + lit aliases + `__DIORAMA_VERSION__` define); the 2D glow section also imports `flights.mod.js`; run wide (`--window-size=1200,900`) |
| landmark-csv-test.html | `LANDMARKCSV PASS 147/147` — GPS-landmark CSV import + per-landmark alignment exclusion + suggested-position repair. Pure `parseLandmarkCsv` (header in any order + extra columns, headerless numeric first row, unrecognized header skipped with a note, quoted labels w/ commas + `""` escapes + embedded newlines, CRLF, blank lines, trimmed cells, one error per bad row while good rows still import, 200-row cap, garbage never throws) + the REAL `Planner` over a fake HaApi (config-test bundle recipe): **with a live fit** rows project via `latLonToPlan` onto exact plan mm and the refit is neutral (pair count grows, θ/tx/ty/fittedScale unchanged, rms stays sub-mm; the fit is snapshotted ONCE so later rows don't drift) — the fixture's calibration pair is derived from its own plan coords via `unprojectMeters`, so the baseline fit is exact; **with no fit** rows import `pendingPlace` in a spaced row at the floor centre and `geoFit()` EXCLUDES them (no poisoning) until placement clears the flag; label-keyed update path (case-insensitive/trimmed, keeps x/y, sets `sampledAt`, clears accuracy+sampleCount, a pending landmark stays pending, in-file duplicates collapse); edit-only (kiosk/view refuse); one import = ONE undo step. **`GeoLandmark.excluded`** (§8 — the "use in alignment" toggle): excluding is fit-identical to DELETING (quality/θ/tx/ty) while the landmark + its coordinates stay in the store, re-including restores the original fit bit-for-bit, and the user's reported scenario is reproduced numerically — two consistent landmarks define a 25° plan rotation, a third with a mis-sampled lat/lon drags the Procrustes θ to ~47° (rms > 1 m, fittedScale ⚠), `residualsMm` stays index-aligned with `geoFit().landmarks` so the worst residual names the culprit (what the sidebar's per-row `off by N m` readout shows), and switching that one landmark off recovers θ = 25.000° / sub-mm rms; the flag survives `_normalizeStore` via undo/redo and keeps `importLandmarksCsv`'s snapshot fit unpoisoned (with a control asserting the same landmark INCLUDED moves the projection). **Suggested position** (§9 — `landmarkSuggestId` / `landmarkSuggestion()` / `applyLandmarkSuggestion()`): the latched landmark's stored lat/lon projected back through the FULL current fit (golden-checked against `latLonToPlan` of the same transform, `distMm` = the gap from the pin) drives the 2D ghost pin + the sidebar card; fail-soft null matrix (no fit / unknown id / uncalibrated / `pendingPlace` / unset latch) with apply refusing in each; apply rounds onto the projection, clears the latch, is ONE undo step (undo/redo round-trip) and strictly reduces the fit error; the latch is runtime-only (cleared by `setUiMode` + `_applyLoadedStore`) and apply refuses in kiosk/view; and the repair flow is asserted end-to-end — an EXCLUDED mis-placed pin gets a suggestion computed from the OTHER landmarks alone (landing on its true plan spot), applying it then re-including leaves θ unchanged with a ~0 residual for that pin. Bundle: `geo.ts` plain (zero-import), planner per the config-test recipe |
| neighborhood-test.html | `NEIGHBORHOOD PASS 95/95` — OpenFreeMap overlay data layer: MVT decode over real z14 tile fixtures, tile math, height/exclusion/align/cap helpers, and the REAL `Planner` fetch/cache/re-extract paths over a fake HaApi + fake tile store. Includes the **radius-scaled render caps** (`buildingCapForRadius` / `roadCapForRadius`: base caps at `CAP_REF_RADIUS_M` 500 m, never below them, 2× at 1000 m, clamped at the 1600/1800 ceilings, monotonic, NaN-safe; `capBuildings` honors a passed cap above the default and defaults to `MAX_BUILDINGS` when omitted) and `tilesForRadius` at the 3 km ceiling (16 z14 tiles at Brooklyn's latitude, ⊇ the 350 m set). Bundles: `mvt-decode.ts` plain, `neighborhood.ts` + `geo.ts`, planner per the config-test recipe (with `fake-neighborhood-store.ts`) |
| neighborhood-render-test.html | `NBHDRENDER PASS 95/95` — the overlay's renderer half: building prisms in the active-floor frame (incl. the asymmetric-footprint front-to-back **mirror catch**), podium `baseMm`, one shared material, `outlineSkip` + no outline shells, self-intersecting rings pre-rejected, flat road/water/landuse patches at y=3/2/1, per-layer color overrides, the road-ribbon cap (default 600, raised/lowered via `cfg.maxRoads`), layer gating, and the **camera-distance-tracking frustum** (`_applyFrustumForRange` records the reach, `_updateDynamicFrustum` tracks it per frame — driven manually here, the page body is synchronous so no `_animate` RAF interleaves): stock triple 10 / 150000 / 45000 at rest and with near content; a 100 m-distant building grows `far` to `camDist + 1.25·req + 30000` with `near = far/15000` and `maxDistance = req·2.2`; **the reported horizon bug** — zooming out PUSHES `far` out (never clips the tiles) and zooming back in shrinks it again, invariant `far ≥ camDist + 1.25·req + 30000` asserted at both poses; **hysteresis** — a ±2 % `camDist` wiggle bumps neither `far` nor the projection-matrix call count (wrapped + counted), a >5 % move does; a 12 M mm req clamps both ceilings (far 13.5e6 / maxDistance 8.5e6, near 900, minDistance still clears it); a realistic 3 km req (reach 3.8e6) keeps `maxDistance = req·2.2` un-clamped, `d·tan(25°) ≥ req` frames the extent, and the invariant still holds when parked AT `maxDistance`; `far/near ≤ 15000` in every state; `resize()` and `setFov()` preserve near/far/maxDistance; null/empty data restores the stock triple EXACTLY and the dynamic updater goes inert afterwards; **the per-source requirement UNION** (§19b — the overlay is no longer the only far-reaching feature: `_recordFrustumReq('nbhd'|'flights', …)` keeps one requirement per source and the far/near widening uses their MAX; flights-only widens `far` but leaves `maxDistance` STOCK, nbhd-only widens both, with both present `far` tracks whichever is larger while `maxDistance` comes from the neighborhood term ALONE, dropping one source keeps the other's widening, and only when BOTH are cleared does the stock triple restore with strict `===` and the updater go inert); sky-dome interaction pin (`depthWrite:false` + `renderOrder −10`, so buildings overdraw it at any far plane) |
| terrain-test.html | `TERRAIN PASS 103/103` — T1 terraces + yard fill + **ground level**. Pure `groundAreaSkirtBase` (nested tiers, hidden skipped, sunken, highest-wins) + `polygonCentroid`; renderer: terrace top at `elevationMm + 4`, skirt only when elevation ≠ 0, angled grass vs vertical rock flare, nested skirt bases, `_groundYAt` inside/outside, yard-fill underlay punched with wall-loop holes, `ground` layer flip, rebuild tracks an elevation change, robot rig re-grounds on a terrace. **`Scene3D.groundLevelMm`** (section G — the SURROUNDINGS move, the house does not): defaults byte-compatible (grid + `_neighborhoodGroup` at y=0, yard fill at y=2, `_groundYAt` outdoors 0); a lowered level drops the backdrop grid, the neighborhood GROUP (building prisms keep their local podium base — it's a whole-group offset) and the yard-fill patch to `2 + level`, makes `_groundYAt` return the level OUTSIDE every wall loop while staying 0 INSIDE, settles an outdoor robot rig onto it, runs the fill continuously UNDER the raised pad (holes punched only at level ≥ 0 — below it they'd expose the grid), re-applies on a neighborhood-ONLY rebuild (dirty-key order safety), falls back to the floor's level when `updateGroundAreas` is called without the arg, clamps at ±10 000 mm, and restores exactly at 0. **Section H — FREE-STANDING content follows the grade** (the one `_itemGroundY` rule: outside every closed wall loop → the surroundings level, else the slab; house-mounted things and everything indoors never move; every case run at −600 AND at 0): an outdoor tree's group base drops while an identical indoor tree stays at 0; an outdoor lawn chair's SitSpot seat rides the grade and a rig that dwells there sits ON the cushion (Δroot = the level); an outdoor ground-area patch stacks on the grade (`level + elev + 4`) with `_groundYAt` agreeing while an indoor patch stays at 4; an outdoor pool's water surface recesses from the grade; a ground-standing lamp + its floor pool follow it while a wall sconce and a wall switch do NOT; an outdoor GPS landmark sprite follows it while the indoor "find my phone" pin does not; and the lawn-text decal follows it (the reported bug). Uses `geometry.mod.js` (same esbuild transpile as rooms-test) |
| bgtext-multi-test.html | `BGTEXTMULTI PASS 102/102` — multi-instance background text (skywriting / banner plane / grass / message train / news chopper) + legacy→`bgTexts` migration + per-entry resolution, over the REAL renderer and a REAL `Planner`. Adds: **ground level** — the train's rails, the tow plane's and chopper's AGL orbit altitude and the skywriting all ride `Scene3D.groundLevelMm` (level 0 byte-identical); **chopper tow point** — the banner Group origin IS its leading TOP corner (planes offset by −len/2, −bh/2), the tow wire's far end coincides with that origin, the cloth trails back + below it, and the sway pivots there (origin invariant under `rotation.y`); **phase persistence** — per-entry-id `_bgTextPhase` (orbit angle / train arc position + wheel spin / sky drift clock) survives a rebuild, so an identical rebuild leaves the plane's and train's horizontal positions bit-identical, a text-change rebuild resumes mid-course rather than restarting the lap, and a FRESH entry id still starts at its build stagger; **tow-aircraft model** — `BgTextEntry.aircraft` builds the banner plane from one of the 8 FLIGHT archetypes via the shared `_buildAircraftModel` (absent/garbage → the classic toy plane, verified by real geometry: mesh count + wing span; ga-high keeps its HIGH-wing tag, widebody has >2× the plan footprint plus underwing pods, the banner still tows readably and trails clear of the longer fuselage, twin-prop spins TWO discs about Z, a `heli` archetype spins its main rotor about Y + tail rotor about X while flying the ordinary BANNER orbit, a jet has no disc at all, and `aircraft` is ignored on a `chopper` entry so the dedicated news build is untouched); **model size** — `BgTextEntry.scale` multiplies the whole rig of every style (plane/chopper assembly, sky sprite, grass decal, train consist ×1.8 base with spacing + wheelR carried along), leaves the flight path/orbit radius alone, clamps 0.5..5 with garbage → 1, composes with ground level AND phase persistence, and survives the per-frame advance. Bundles: `planner.mod.js` per the config-test recipe |
| floors-view-test.html | `FLOORSVIEW PASS 67/67` — floor ordering + visibility + **floor-switch viewport retention**. Pure `floorsDisplayOrder` / `peekFloors` + the REAL `Planner` over a fake HaApi (config-test bundle recipe; imports ONLY `geometry.mod.js` + `planner.mod.js`, no built `dist/` needed): display order is the reversed canonical array, `moveFloor` ▲/▼ mapping, plan nudge = `translateFloorContent` + one undo step, tri-state show/peek/hide cycling + persistence. **Retention** (§8–10): `viewCenterFitsFloor` guard band — the floor rect inflated by `VIEW_RETAIN_MARGIN_FRAC` (0.5) × `max(w, d)` on every side, boundary-exact both signs, margin keyed off the LARGER dimension in BOTH axes, NaN rejected; `floorSwitchCameraDelta` goldens — equal dims → exact `{0,0}`, growing floor → `dx = +(fw′−fw)/2` and `dz = −(fd′−fd)/2`, round-trip cancels, and the sign convention asserted directly against `_w(wx, wy, h) = (fw/2 − wx, h, wy − fd/2)` for a real world point; and `switchFloor` itself — same-dims switch retains `viewCenter`/`zoom` EXACTLY (over repeated switches, no drift), a never-panned `null` centre stays null, a different-dims switch retains while the centre is inside the band and resets to fit when it's far outside (unknown floor id likewise), while `resetView()` / `newConfig` / `switchConfig` / a fresh load still reset (a different CONFIG genuinely is a different plan) |
| localstate-test.html | `LOCALSTATE PASS n/n` — local control of UNBOUND objects (`entity_id: null` + `localState`): door/window open via localState (green sash emissive), unbound bulb lit + off dim + fireplace lit, and a locally-`'on'` TV gating `watch_tv` (off ≠ watch_tv). Exercises `itemState()` + the `hasEntity` (entity-or-local) flag; the same `stateProvider` closures carry it through |

| mechanical-test.html | `MECHANICAL PASS 100/100` — mechanical & utility appliances (10 `cat:'appliance'` kinds: water_heater / air_handler / floor_radiator / wall_radiator / boiler / ac_condenser / heat_pump / sump_pump / recirc_pump / printer_3d). Pure `geometry.ts`: pinned footprint defaults, `isMechanicalApplianceKind` / `isPumpKind` / `mechanicalBindDomains` / `defaultFurnitureElevation`, `printerProgress` (bare-numeric state → progress attributes → null; clamped; never throws on a non-string state) and the `mechanicalRun` GLOW MATRIX (water heater's operation modes all heat; air handler / heat pump tri-state through `hvacAirflow` with `hvac_action` beating the mode and `action:'idle'` reading as honest-dark; single-purpose plant pinned to its one color — a condenser bound to a plain switch still glows cool; pumps never glow; printer runs only strictly between 0 and 100 %). Renderer: all 10 build with composite geometry + outline shells, blob-shadow policy (floor-standing yes / wall-hung `elevation 200` no), the heat-red / cool-blue / fan-white emissive matrix per resolved state, the generic green in-use LED SUPPRESSED on all 10, condenser + heat-pump rotors registered in `_floorFans` with `rps > 0` only while running (blades advance while cooling, ease to rest when off, never oscillate), pump flow-texture CLONE lifecycle (one clone per RUNNING pump, offset scrolls, disposed on rebuild with a dispose-count probe, a stopped pump enrols none so its water is frozen, the shared source survives), the 3D printer (print height tracks a bound progress 25→75 and hides at 0 %, gantry head sweeps ±travel while printing and is frozen when off, an on/off binding grows on the deterministic loop, `printProgressEntity` overrides an on/off primary, head phase survives a `_keyFloor` rebuild), unbound `localState` control, and `clearTransientGroups` teardown. 2D: `drawFurniturePrimitiveLocal` no-throw + non-blank glyph per kind. Bundles `geometry.ts` + `canvas-render.ts` via `esbuild --bundle` beside `dist/*` (the climate-appliance-test recipe) |
| orient-interact-test.html | `ORIENT PASS 23/23` — **orientation-gated avatar interactions** + the tend_plant pose. A piece with a functional front (`hasFunctionalFront` = the existing `frontArrow !== false` signal; front is local −Z) may only be engaged from that side: a rig BEHIND a fridge never opens its door (proximity trigger) and never captures its `forage_fridge` anchor, while a rig in front does both; the activity anchor's stand point is asserted to lie in the front halfspace; a SYMMETRIC piece (plant, `frontArrow: false`) still captures from any side and carries no front normal. The AI interaction goal lands in the item's front halfspace and the toggle only FIRES from the front (cooldowns still arm from behind, so there is no retry storm). Pose section: during an engaged `tend_plant` the pelvis (leftHip world pos) moves < 6 mm while the working hand travels > 40 mm — the old pose animated the ROOT PITCH with both arms frozen, which read as a pelvic thrust; `browse_bookshelf` carries the same pin. Needs only the built `dist/`.

| weather-fx-test.html | `WFX PASS <case> n/n` per `?c=` case — the 3D weather effects (W2/W3) + the phase-3 sky. `?c=pouring 6/6` (one rain Points cloud, particles move, Y stays in the 0–4000 recycle band, no fog), `?c=fog 12/12` (FogExp2 eased in/out + the zoom-compensation contract), `?c=lightning 5/5`, `?c=sunny 4/4` (everything torn down), `?c=alert 7/7` (DC-D beacon pulse/color/decay), `?c=w3 20/20` (coverage/visibility/gust/frost/puddles/sun-position **plus the cloud-shadow SHADE contract** — the shared texture is pure-black rgb with a soft alpha ramp so a NormalBlending decal is an exact multiply by (1−a) and can never lighten dark ground, material normal-blended + untinted + subtle, and the shade scales with cloud coverage), `?c=sky 45/45` (dome/sun/moon/starfield: sun target az/elev, moon phase textures pixel-sampled, overcast vs clear dome, easing — **plus the sun-disc GLOW contract**: warm `r>g>b` core, small fast-falling core + wide soft halo with no hard edge, AdditiveBlending, a high sun's peak dimmed below a low sun's — the **daylight star rule**: a live sun above the horizon suppresses the starfield whatever the preset says, and it returns once the sun sets — and the **`sunDisc` per-effect key**: absent effects map (a stale three-view) still shows the disc, the key off zeroes the opacity TARGET so the sprite EASES out (still on screen at ~0.1 s, gone after the τ≈2 s fade) and returns when re-enabled, while the dome/starfield/moon and the separate `sunPosition` sun-light target are untouched) |
| sky-real-test.html | `SKYREAL PASS 32/32` — the astronomically-correct catalog sky (real stars/constellation lines/planets/moon at true alt-az). Draw counts match a `skySnapshot` from the pure `sky-astro.mod.js` bundle at an injected epoch (`setSkyEpochOverride`), buffers rotate across a 6 h epoch, the real moon azimuth differs from the decorative `sunAz+180`, no-observer falls back to the decorative starfield (no horizon ring), `skyBackdrop:false` hides everything — **plus the daylight gate**: the whole catalog group goes `visible=false` with stars/lines/planets all at ~0 opacity for a plain `day` preset, for an OVERCAST day (preset-downgraded to `dusk` with the sun still up — the case that used to paint constellations over a bright daytime dome), and for a manual `night` preset while the sun is up; sunset restores it (no one-way latch) and civil twilight (−3°) is a partial ramp, not a switch. Build `sky-astro.mod.js` first (`npx esbuild src/sky-astro.ts --bundle --format=esm`) |

These were written incrementally by coding agents; fixtures reference the
entity-shape the renderer expects (`stateProvider`-style closures), not a
live HA connection.
