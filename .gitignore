node_modules/
dist/
*.tsbuildinfo
# local build artifacts / unzipped release copies
diorama.zip
/diorama/
# editor / personal
*.code-workspace
.claude/settings.local.json
.DS_Store
# machine-local deploy target (private)
deploy.local.json
# generated documentation gallery (npm run docs:gallery) — regenerable, not committed
/docs-site/
# private infrastructure notes (remotes, hosts) — never push
docs/STATUS.local.md
# generated test-harness bundles — every test page documents its own esbuild
# regen command in its header; ~27 pages write the SAME planner.mod.js name, so
# a committed copy is only ever "whatever ran last" (a real flake source)
test-pages/*.mod.js
